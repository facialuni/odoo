<odoo>
	<template id="portal_my_home_menu_invoice" name="Portal layout : invoice menu entries" inherit_id="portal.portal_layout" priority="30">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'invoice'" class="active">
                Invoices
            </li>
            <li t-if="invoice">
                <t t-esc="invoice.name"/>
            </li>
        </xpath>
    </template>

    <template id="portal_my_home_invoice" name="Portal My Home : invoice entries" inherit_id="portal.portal_my_home" priority="30">
        <xpath expr="//ul[hasclass('o_portal_docs')]" position="inside">
            <li t-if="invoice_count" class="list-group-item">
                <span class="badge" t-esc="invoice_count"/>
                <a href="/my/invoices">Invoices</a>
            </li>
        </xpath>
    </template>

    <template id="portal_my_invoices" name="My Invoices and Payments">
      <t t-call="portal.portal_layout">
        <h3>Invoices
            <t t-call="portal.portal_searchbar"/>
        </h3>
        <t t-if="not invoices">
            <p>There are currently no invoices and payments for your account.</p>
        </t>
        <div t-if="invoices" class="panel panel-default">
            <div class="table-responsive"><table class="table table-hover o_my_status_table">
                <thead>
                  <tr class="active">
                    <th>Invoice #</th>
                    <th>Invoice Date</th>
                    <th class='hidden-xs'>Due Date</th>
                    <th></th>
                    <th>Amount Due</th>
                  </tr>
                </thead>
                <t t-foreach="invoices" t-as="invoice">
                    <tr>
                        <td>
                            <a t-attf-href="/my/invoices/#{invoice.id}" t-att-title="invoice.number"><t t-esc="invoice.number"/></a>
                        </td>
                        <td><span t-field="invoice.date_invoice"/></td>
                        <td class='hidden-xs'><span t-field="invoice.date_due"/></td>
                        <td>
                            <t t-if="invoice.state == 'open'">
                                <span class="label label-info"><i class="fa fa-fw fa-clock-o"></i><span class="hidden-xs"> Waiting for Payment</span></span>
                            </t>
                            <t t-if="invoice.state == 'paid'">
                                <span class="label label-default"><i class="fa fa-fw fa-check"></i><span class="hidden-xs"> Paid</span></span>
                            </t>
                            <t t-if="invoice.state == 'cancel'">
                                <span class="label label-default"><i class="fa fa-fw fa-remove"></i><span class="hidden-xs"> Cancelled</span></span>
                            </t>
                        </td>
                        <td><span t-field="invoice.residual" t-options='{"widget": "monetary", "display_currency": invoice.currency_id}'/></td>
                    </tr>
                </t>
            </table></div>
            <div t-if="pager" class="o_portal_pager text-center">
                <t t-call="portal.pager"/>
            </div>
        </div>
      </t>
    </template>


    <template id="invoice_template_portal_page" name="Invoice Portal Template">
        <t t-call="portal.portal_layout">
            <div class="container">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <div class="row">
                            <div class="col-md-12">
                                <h4>
                                    <t t-call='portal.record_pager'/>
                                    Invoice
                                    <span t-esc="invoice.name"/>
                                </h4>
                            </div>
                        </div>
                    </div>
                    <div class="panel-body">
                        <div class="mb8">
                            <strong>Date:</strong> <span t-field="invoice.date_invoice" t-options='{"widget": "date"}'/>
                        </div>
                        <div class="row">
                            <div class="col-md-6">
                                <strong>Product</strong>
                            </div>
                            <div class="hidden-sm hidden-xs col-md-2 text-right">
                                <strong>Unit Price</strong>
                            </div>
                            <div class="hidden-sm hidden-xs col-md-2 text-right">
                                <strong>Quantity</strong>
                            </div>
                            <div class="hidden-sm hidden-xs col-md-2 text-right">
                                <strong>Subtotal</strong>
                            </div>
                        </div>
                        <t t-foreach="invoice.invoice_line_ids" t-as="line">
                            <div class="row">
                                <div class="visible-lg col-lg-1 text-center">
                                    <span t-field="line.product_image" t-options="{'widget': 'image', 'class': 'img-rounded', 'style':'width: 48px;height:48px'}" />
                                </div>
                                <div id='product_name' class="col-lg-4 col-sm-6 col-xs-4">
                                    <span t-esc="line.product_id.name"/>
                                </div>
                                <div class="col-xs-3 text-right">
                                    <span t-field="line.price_unit" t-options='{"widget": "monetary", "display_currency": invoice.currency_id}'/>
                                </div>
                                <div class="col-xs-2 text-right">
                                    <span t-esc="line.quantity"/>
                                </div>
                                <div class="col-sm-2 col-xs-3 text-right">
                                    <span t-field="line.price_subtotal" t-options='{"widget": "monetary", "display_currency": invoice.currency_id}'/>
                                </div>
                            </div>
                        </t>
                        <hr/>
                        <div class="row">
                            <div class="col-md-6">
                                <div>
                                    <strong>Contact</strong>
                                </div>
                                <div t-field="invoice.user_id.partner_id" t-options='{"widget": "contact", "fields": ["email", "phone"]}'/>
                                <br/>
                            </div>
                            <div class="col-md-6">
                                <div class="row">
                                    <div class="col-md-9 col-xs-8 text-right">
                                        Untaxed Amount:
                                    </div>
                                    <div class="col-md-3 col-xs-4 text-right">
                                        <!-- <span t-field="order.amount_untaxed" t-options='{"widget": "monetary", "display_currency": invoice.currency_id}'/> -->
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="col-md-9 col-xs-8 text-right">
                                        Taxes:
                                    </div>
                                    <div class="col-md-3 col-xs-4 text-right">
                                        <!-- <span t-field="order.amount_tax" t-options='{"widget": "monetary", "display_currency": invoice.currency_id}'/> -->
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="col-md-9 col-xs-8 text-right">
                                        <strong>Total:</strong>
                                    </div>
                                    <div class="col-md-3 col-xs-4 text-right">
                                        <strong>
                                            <!-- <span t-field="order.amount_total" t-options='{"widget": "monetary", "display_currency": invoice.currency_id}'/> -->
                                        </strong>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="oe_structure mb32"/>
        </t>
    </template>

</odoo>